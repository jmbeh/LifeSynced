# Supabase Configuration (Required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Password Protection (Required for production)
# Users must enter this password to view the calendar
# Change anytime in Vercel dashboard → Settings → Environment Variables
APP_PASSWORD=your-family-password

# Personal Events Reveal Password (Required for reveal capability)
# Personal calendar event names are ALWAYS masked as "[Personal Event]" by default
# Set this password to enable the reveal feature (only you would know this)
# Overlap detection still works with masked events
PERSONAL_REVEAL_PASSWORD=your-reveal-password

# Calendar ICS URLs (Required - at least one)
# Outlook ICS feed URL (get from Outlook on the Web → Calendar → Share → ICS link)
OUTLOOK_ICS_URL=

# iCloud Calendar ICS URLs (use _1, _2, etc. for multiple calendars)
# Get from iCloud.com → Calendar → Share → Public Calendar
APPLE_CALENDAR_ICS_URL_1=
APPLE_CALENDAR_ICS_URL_2=
